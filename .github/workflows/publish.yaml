on:
  workflow_dispatch: {}
  release:
    types:
      - released

jobs:
  publish-base:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: false
      - uses: actions/setup-java@v3
        with:
          distribution: zulu
          java-version: 17
          architecture: x64
      - uses: gradle/gradle-build-action@v2
        with:
          arguments: --no-daemon :base:publish -Pkr328appUsername=${{ secrets.PUBLISH_USERNAME }} -Pkr328appUsername=${{ secrets.PUBLISH_PASSWORD }}
  publish-linux-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
      - uses: actions/setup-java@v3
        with:
          distribution: zulu
          java-version: 17
          architecture: x64
      - run: |
          sudo apt-get update -y
          sudo apt-get install -y gcc libdbus-1-dev libx11-dev cmake ninja-build
      - uses: gradle/gradle-build-action@v2
        with:
          arguments: --no-daemon :jni:publish -Pkr328appUsername=${{ secrets.PUBLISH_USERNAME }} -Pkr328appUsername=${{ secrets.PUBLISH_PASSWORD }}
  publish-linux-i386:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
      - uses: actions/setup-java@v3
        with:
          distribution: zulu
          java-version: 17
          architecture: x86
      - run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update -y
          sudo apt-get install -y gcc-multilib libdbus-1-dev:i386 libx11-dev:i386 cmake ninja-build
          sudo apt-get install -y zlib1g:i386
      - uses: gradle/gradle-build-action@v2
        with:
          arguments: --no-daemon :jni:publish -Pkr328appUsername=${{ secrets.PUBLISH_USERNAME }} -Pkr328appUsername=${{ secrets.PUBLISH_PASSWORD }}
        env:
          PKG_CONFIG_PATH: /usr/lib/i386-linux-gnu/pkgconfig
  publish-windows-amd64:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
      - uses: actions/setup-java@v3
        with:
          distribution: zulu
          java-version: 17
          architecture: x64
      - uses: lukka/get-cmake@latest
      - run: |
          C:\msys64\usr\bin\bash -lc "pacman --noconfirm -S mingw-w64-x86_64-gcc"

          Add-Content -Path $env:GITHUB_PATH -Value "C:\msys64\mingw64\bin"
      - uses: gradle/gradle-build-action@v2
        with:
          arguments: --no-daemon :jni:publish -Pkr328appUsername=${{ secrets.PUBLISH_USERNAME }} -Pkr328appUsername=${{ secrets.PUBLISH_PASSWORD }}
        env:
          CC: C:\msys64\mingw64\bin\gcc.exe
  publish-windows-i386:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
      - uses: actions/setup-java@v3
        with:
          distribution: zulu
          java-version: 17
          architecture: x86
      - uses: lukka/get-cmake@latest
      - run: |
          C:\msys64\usr\bin\bash -lc "pacman --noconfirm -S mingw-w64-i686-gcc"
          
          Add-Content -Path $env:GITHUB_PATH -Value "C:\msys64\mingw32\bin"
      - uses: gradle/gradle-build-action@v2
        with:
          arguments: --no-daemon :jni:publish -Pkr328appUsername=${{ secrets.PUBLISH_USERNAME }} -Pkr328appUsername=${{ secrets.PUBLISH_PASSWORD }}
        env:
          CC: C:\msys64\mingw32\bin\gcc.exe
